# SPDX-FileCopyrightText: Copyright 2023 The Minder Authors
# SPDX-License-Identifier: Apache-2.0

{{/*
Test that the deployment completed successfully.

Currently disabled.
*/}}
{{/*
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minder.fullname" . }}-test-connection"
  labels:
    {{- include "common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
*/}}
